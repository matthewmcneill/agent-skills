# Kotlin Android — House Style Reference

> [!NOTE]
> This reference file is a placeholder. It will be populated when the first
> Kotlin Android project is started. Refer to the
> [Kotlin official style guide](https://kotlinlang.org/docs/coding-conventions.html)
> and [Android Kotlin style guide](https://developer.android.com/kotlin/style-guide)
> as a baseline until this document is fully developed.

---

## TODO

- [ ] Module header template for Kotlin files (KDoc format)
- [ ] KDoc documentation examples (`@param`, `@return`, `@throws`, `@sample`)
- [ ] Kotlin naming conventions:
  - Classes, objects, interfaces → PascalCase
  - Functions, properties → camelCase
  - Constants (`const val`) → SCREAMING_SNAKE_CASE
  - Backing properties → `_camelCase`
  - Packages → lowercase, no underscores
- [ ] Android-specific patterns:
  - ViewModel, Repository, UseCase naming
  - Resource naming (`snake_case` for XML layouts, drawables, strings)
  - Compose component naming (PascalCase functions)
- [ ] Flow comment patterns for Kotlin (coroutines, `when` blocks)
- [ ] Arcane logic examples (scope functions, delegation, reified generics)
